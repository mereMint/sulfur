# Installation Wizard Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│           🪟 Sulfur Bot Installation Wizard                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 1: Check Prerequisites                                │
│  ───────────────────────────────────────────────────────    │
│                                                             │
│  ✓ Python 3.8+        → Download if missing                │
│  ✓ Git                → Download if missing                │
│  ✓ MySQL/MariaDB      → Install guide if missing           │
│                          Start if not running               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 2: Bot Configuration (.env)                           │
│  ───────────────────────────────────────────────────────    │
│                                                             │
│  Interactive prompts for:                                   │
│  • Discord Bot Token      → Opens developer portal          │
│  • Gemini API Key         → Opens AI studio                │
│  • OpenAI API Key (opt)   → Opens platform page            │
│  • Database settings      → Smart defaults provided         │
│  • Bot prefix & owner ID  → Optional configuration          │
│                                                             │
│  Creates .env file with all settings                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 3: Database Setup                                     │
│  ───────────────────────────────────────────────────────    │
│                                                             │
│  • Checks if database exists                                │
│  • Runs setup_wizard.py if needed                           │
│  • Creates database and user                                │
│  • Applies migrations (apply_migration.py)                  │
│  • Tests connection                                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 4: Install Dependencies                               │
│  ───────────────────────────────────────────────────────    │
│                                                             │
│  • Creates virtual environment (venv)                       │
│  • Activates venv                                           │
│  • Upgrades pip                                             │
│  • Installs from requirements.txt:                          │
│    - discord.py                                             │
│    - mysql-connector-python                                 │
│    - Flask + Flask-SocketIO                                 │
│    - waitress                                               │
│    - aiohttp, python-dotenv                                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 5: Test Setup                                         │
│  ───────────────────────────────────────────────────────    │
│                                                             │
│  Runs test_setup.py to verify:                              │
│  ✓ Environment variables set                                │
│  ✓ Configuration files valid                                │
│  ✓ Database connection works                                │
│  ✓ API keys are valid                                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│  Step 6: Create Shortcuts                                   │
│  ───────────────────────────────────────────────────────    │
│                                                             │
│  Desktop shortcuts created:                                 │
│  🚀 Start Sulfur Bot        → Runs start.ps1               │
│  🌐 Sulfur Web Dashboard    → Opens localhost:5000         │
│  📁 Sulfur Bot Folder       → Opens installation dir       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│              ✅ Installation Complete! 🎉                   │
│                                                             │
│  Option to start bot immediately or exit                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## Color-Coded Feedback

The wizard uses visual feedback throughout:

- 🟦 **Blue Headers** - Section titles
- 🟩 **Green ✓** - Success messages
- 🟨 **Yellow ⚠** - Warnings (usually safe to continue)
- 🟥 **Red ✗** - Errors (need attention)
- ⬜ **White ℹ** - Information and guidance

## Skip Options

Advanced users can skip certain steps:

```powershell
# Skip prerequisite checks
.\install_wizard.ps1 -SkipPrerequisites

# Skip database setup
.\install_wizard.ps1 -SkipDatabase

# Skip dependency installation
.\install_wizard.ps1 -SkipDependencies

# Combine multiple skips
.\install_wizard.ps1 -SkipPrerequisites -SkipDependencies
```

## Error Handling

At each step, the wizard:
- ✅ Validates prerequisites before proceeding
- ✅ Provides helpful error messages
- ✅ Offers to download missing components
- ✅ Allows user to continue or abort
- ✅ Tracks completion status

## What Happens After Installation

```
Start Bot (.\start.ps1 or desktop shortcut)
         │
         ▼
┌────────────────────────┐
│  maintain_bot.ps1      │
│  ────────────────      │
│  • Starts web          │
│    dashboard           │
│  • Starts Discord bot  │
│  • Auto-updates        │
│  • Auto-commits        │
│  • Auto-backups        │
└────────────────────────┘
         │
         ├──→ Web Dashboard: http://localhost:5000
         │    • Real-time logs
         │    • AI usage stats
         │    • Config editor
         │    • Database viewer
         │
         └──→ Discord Bot
              • Responds to commands
              • AI conversations
              • Games (Werwolf)
              • Economy system
```

## Typical Installation Time

- **With fast internet:** 5-10 minutes
- **With slow internet:** 10-15 minutes
- **Manual setup:** 20-30 minutes

The wizard automates everything that can be automated, making setup as quick and painless as possible!
